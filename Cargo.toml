[package]
name = "nix-bindings"
description = "Rust binding for Nix, the build tool"
edition = "2024"
version = "2.28.4"                                   # after the Nix version bindings were made with.

[lib]
path = "src/lib.rs"

[build-dependencies]
bindgen = "0.72.0"
pkg-config = "0.3.32"

[dev-dependencies]
serial_test = "2"
